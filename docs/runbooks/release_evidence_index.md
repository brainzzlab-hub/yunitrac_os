# Release Evidence Index (Enterprise + Lite)

## Commands (run from repo root)
- `./scripts/prove_enterprise.sh`
- `./scripts/prove_lite.sh`
- Optional baseline: `./scripts/prove.sh`

Each must print only PASS/FAIL lines for gates; no reasons or internal details are exposed.

## Expected gates (must PASS)
- cargo fmt
- CARGO_NET_OFFLINE=true cargo clippy -- -D warnings
- CARGO_NET_OFFLINE=true cargo test
- CARGO_NET_OFFLINE=true cargo build --release
- prove_enterprise.sh
- prove_lite.sh

## Artifact map (relative paths)
- Lite:
  - `artifacts/proof_report_lite.json`
  - `artifacts/lite/scope_manifest.json`
  - `artifacts/lite/evidence_bundle/` (commands.txt, required_hashes.json, policy_checks.json)
  - `artifacts/lite/tmp_B/`, `artifacts/lite/tmp_C/` (scratch, excluded from equality)
- Enterprise:
  - `artifacts/proof_report_enterprise.json`
  - `artifacts/enterprise/runA/`, `runB/`, `runC/` (audit/metrics/security/outputs/hashes/frames)
  - `artifacts/enterprise/evidence_bundle/` (manifest, comparisons, bucket_policy, chain_verification, approvals, replay)
- Canonical footer for rendered block (all profiles): `Generated by yunitrack_os | Stateless | No data retained`

## Customer-facing checklist
- Read: `docs/runbooks/customer_pitch_onepager.md`
- Read: `docs/runbooks/deployment_matrix.md`
- Lite SecCode bounds: `docs/specs/seccode_lite.md`
- Show: PASS output of prove_enterprise.sh (and prove_lite.sh if requested) and point to artifact map above.

## Determinism statement
- Mechanically enforced under stated assumptions: B==C byte equality on declared REQUIRED_EQUALITY_SET (Enterprise: outputs/audit/metrics/security/hashes; Lite: scope_manifest set).
- Exclusions (Lite EXCLUDED_SET): `target/**`, `artifacts/lite/tmp_*/**`, `**/*.log` (ordering variance), and other non-canonical scratch outputs.

## GDPR / anti-covert-channel statement
- AUDIT and SECURITY buckets are content-free (no payload content/PII; SecCode only).
- METRICS are numeric-only, bounded, quantized, and rate-limited; no covert channel.
